"""CLI package: the ``llmode`` command-line client.

The CLI is a thin HTTP client of the daemon's management API (plus a ``serve``
command that launches the daemon and a ``doctor`` that probes backends locally).
"""
