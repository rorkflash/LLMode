"""HTTP/WebSocket routers for the daemon.

  * management — /api/* control-plane endpoints.
  * openai     — /v1/* OpenAI-compatible inference proxy.
  * events     — /api/events WebSocket for live metrics.
"""
